export interface TopBlogs {
    id: string;

    title: string;

    comments: number;

    published: boolean;

    authorId: string;

    authorName: string;

    like: number;

    createdAt: string;
}